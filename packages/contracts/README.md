# Versioned Contracts

Reserved for typed/versioned cross-boundary contracts.

Rules: version externally meaningful contracts; incompatible change requires version bump; AI proposals never equal executable live orders; BrokerAdapter/safety contracts arrive only when their implementation phase requires them. Phase 0 invents no production contract.
